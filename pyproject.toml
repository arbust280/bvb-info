[project]
name = "bvb-scraper"
version = "1.0.0"
description = "Production ETL pipeline for Bucharest Stock Exchange (BVB) data"
requires-python = ">=3.11"

[tool.black]
line-length = 100
target-version = ["py311"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
